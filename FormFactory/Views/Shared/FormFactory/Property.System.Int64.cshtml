@using FormFactory
@model PropertyVm
<input  @Html.Raw(Model.IsWritable ? "" : "readonly=\"readonly\"") class="input-large" id="@Model.Id" name="@Model.Name" size="30" type="text" value="@(Model.Value ?? "")" />
