@using FormFactory
@using FormFactory.AspMvc
@using FormFactory.Components
@model PropertyVm
@{
    var model = (SubmitButton) Model.Value;

}
<input type="submit" class="btn btn-primary" value="@(model.Text)" />
