@using FormFactory
@model PropertyVm
@using FormFactory.AspMvc
<input @FormFactory.ViewHelper.Readonly(Model) @FormFactory.ViewHelper.Disabled(Model) @Html.UnobtrusiveValidation(Model)  class="input-large" id="@Model.Id" name="@Model.Name" size="30" type="text" value="@(Model.Value ?? "")" />
